---
ansible-navigator:
  execution-environment:
    enabled: True
    container-engine: docker
    volume-mounts:
      - dest: /runner/ansible-puzzle-labs/.ssh # for ssh key
        src: $HOME/ansible-puzzle-labs/.ssh

  ansible:
    config:
      path: $HOME/ansible-puzzle-labs/ansible/ansible.cfg
    cmdline: "--forks 20"

  color:
    enable: True

  logging:
    level: info
    append: True
    file: logs/log.txt

  playbook-artifact:
    enable: True
    save-as: artifacts/{playbook_name}-artifact-{time_stamp}.json
